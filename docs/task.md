# Work log

Record of work actually carried out, derived from the plan agreed at the start of each
session. Design decisions that deviate from [`PLAN.md`](./PLAN.md) or [`TODO.md`](./TODO.md)
are recorded separately in [`adr.md`](./adr.md).

> **Issue links**: this repository is not yet under version control and has no configured
> remote, so entries below cannot link to issues or work items. Once a remote exists, link
> each entry to its issue.

---

## 2026-07-25 — Migrate the build system to MSBuild and MSTest (documentation phase)

**Trigger**: review of the repository template generated by GitHub Copilot, plus a
requirement to build with Visual Studio 2026 (MSBuild) rather than CMake, and to
standardise unit tests on MSTest.

### Review findings

The template turned out to be **documentation only**. None of the following existed:
`CMakeLists.txt`, `src\`, `tests\`, any `.github\workflows\`, `.editorconfig`,
`.clang-format`, `.clang-tidy`, `SECURITY.md`, or even a `.git` directory. Several
documents described infrastructure that was never created.

Concrete defects found:

| ID | Defect |
|---|---|
| A1 | CMake build instructions duplicated across `README.md`, `CONTRIBUTING.md`, `AGENTS.md` §4 |
| A2 | `CMakeLists.txt` listed in the repository trees in `AGENTS.md` §3 and `PLAN.md` §5 |
| A3 | "CMake 3.25 or later" listed as a development requirement |
| A4 | `TODO.md` M0 opened with "Create the CMake project" |
| A5 | **Contradiction**: "MSVC toolset v145" alongside "Visual Studio 2022/2026". v145 requires VS2026; VS2022 provides v143 |
| A6 | Documented run path `.\build\Debug\syncwingetlink.exe` does not match MSBuild's output layout |
| B1 | Test framework left undecided as "Catch2 or GoogleTest" |
| B2 | `ctest` used as the test command in four files |
| B3 | **Design defect**: a C++ MSTest project is a DLL and cannot link an EXE's objects, so the single-executable layout in `PLAN.md` §5 was untestable |
| C1 | `SECURITY.md` absent |
| C2 | `.editorconfig` / `.clang-format` / `.clang-tidy` absent although the coding standards mandate them |
| C3 | `CONTRIBUTING.md` linked to a non-existent `CONTRIBUTING_ja.md` |
| C4 | `OWNER` placeholder left unreplaced in `.github/ISSUE_TEMPLATE/config.yml` |
| C5 | `AGENTS.md` §4 asserted that CI builds and tests x64/arm64, but no workflow existed |
| C6 | Alias regex sample differed between `README.md` (`x86`) and `PLAN.md` (`i686`) |
| C7 | Definition of Done duplicated in `AGENTS.md` §10 and `PLAN.md` §11 with differing content |
| C8 | `.gitignore` carried a CMake section and lacked NuGet / test-result entries |
| C9 | No Dependabot configuration |
| C10 | No `task.md` / `adr.md` |

### Completed in this session

Documentation and repository configuration only. **No program code, project files, or
solution were created** — that was explicitly deferred.

- Created `docs/adr.md` with ADR-0001 (MSBuild), ADR-0002 (MSTest + static-library split),
  ADR-0003 (CRT linkage, proposed/unverified), ADR-0004 (flat `src\` layout), and a list
  of open items.
- Created `docs/task.md` (this file).
- Rewrote the build system references throughout `README.md`, `CONTRIBUTING.md`,
  `AGENTS.md`, `PLAN.md`, `TODO.md`, and `.github/PULL_REQUEST_TEMPLATE.md`.
- Resolved defects A1–A6, B1–B3, C1–C4, C6–C10.
- Added `SECURITY.md`, `.editorconfig`, `.clang-format`, `.clang-tidy`,
  `.github/dependabot.yml`, `CONTRIBUTING_ja.md`, and the `tools/` placeholder-replacement
  scripts.
- Updated `.gitignore` for MSBuild/NuGet and dropped the CMake section.

### Environment verified

Confirmed on the maintainer's machine that Visual Studio 2026 Enterprise (install
version 18) provides platform toolset **v145** for both x64 and ARM64, ARM64 cross
compilers, Windows SDK 10.0.26100.0, `CppUnitTest.h` with per-architecture libraries,
`vstest.console.exe`, and the native C++ test adapter. Details are in ADR-0001 and
ADR-0002.

### Deliberately not done

| Item | Reason |
|---|---|
| `syncwingetlink.sln` and the three `.vcxproj` files | Deferred at the maintainer's request; documentation first |
| `src\` and `tests\` sources | Same |
| `.github/workflows/ci.yml` | Would reference a solution that does not exist yet and fail on the first run. Create it together with the projects |
| `git init` / issue / topic branch | The maintainer chose not to place the repository under version control in this session |

---

## 2026-07-25 (continued) — Localized docs, dependency policy, and skill extraction

**Trigger**: follow-up instructions from the maintainer after reviewing the first pass.

### Completed

- **Deleted** `Directory.Build.props` and `props\syncwingetlink.common.props`. They had
  been written before the scope narrowed to documentation and were orphaned — nothing
  imported them. Their intended content survives as ADR-0001 through ADR-0003, which is
  enough to re-derive them when the projects are created.
- **Updated the localized documents**, explicitly authorised this time, overriding the
  standing `AGENTS.md` prohibition. `README_ja.md`, `PLAN_ja.md`, and `TODO_ja.md` now
  match their English counterparts; `rules_ja.md` and `com-api_ja.md` needed no build
  changes. `README_ja.md` also carried the same `x86`/`i686` regex defect as `README.md`
  (defect C6) and was corrected.
- **Extracted the C++ rules into the `cpp-msbuild` skill** (ADR-0006). `AGENTS.md` §4 and
  §5 shrank to summaries with a pointer; the detail now lives in
  `.github/skills/cpp-msbuild/SKILL.md`, mirrored into `.claude/skills/` and
  `.codex/skills/` by `tools/sync-skills.sh` / `tools/Sync-Skills.ps1`.
- **Added two Definition of Done conditions** to `AGENTS.md` §10, `PLAN.md` §11, and
  `PLAN_ja.md`: all unit tests green, and no dependency with a known vulnerability.
- **Recorded the library policy** as ADR-0005: standard library and Windows APIs first,
  third-party dependencies as a justified last resort.
- **Fixed `.gitignore`**, which excluded `.claude/` and `.codex/` outright and would have
  made the new skill mirrors uncommittable.

### Verified

- Both sync scripts were exercised in all modes — `--dry-run`, `--check` (correctly failed
  with mirrors missing, correctly passed after syncing), and a real sync. The bash and
  PowerShell implementations agree.
- The `.gitignore` negation patterns were checked with `git check-ignore` in a throwaway
  repository: skill files are tracked, local agent state stays ignored.
- The placeholder-replacement scripts were exercised in dry-run and error paths, and the
  awk literal-substitution logic was verified independently.

### Not verified

- Whether OpenAI Codex and GitHub Copilot actually discover skills at `.codex/skills/` and
  `.github/skills/`. The layout follows the documented Claude Code convention; the other
  two were not confirmed against vendor documentation. See the caveat in ADR-0006.
- ~~Whether `NuGetAudit` engages for a C++ `.vcxproj`.~~ Moot — NuGet was dropped the same
  day in favour of vcpkg; see the next entry and ADR-0007.

---

## 2026-07-25 (continued) — vcpkg replaces NuGet

**Trigger**: the maintainer specified vcpkg as the package manager, not NuGet.

### Completed

- **ADR-0007**: vcpkg in manifest mode is the package manager; NuGet is dropped for C++
  dependencies. Triplet-to-CRT mapping documented, tying into ADR-0003.
- Removed the `nuget` entry from `.github/dependabot.yml`, replaced the `.gitignore` NuGet
  section with vcpkg entries, and rewrote section 5 of the `cpp-msbuild` skill.
- Updated `TODO.md` / `TODO_ja.md`, and superseded ADR-0005's NuGet parts plus open
  item 2.

### Environment verified

| Finding | Detail |
|---|---|
| vcpkg | **Bundled with VS2026** at `VC\vcpkg\vcpkg.exe`, version `2026-05-27-d5b6777d`. No bootstrap needed. |
| C++/WinRT headers | **Ship in the Windows SDK**: `Include\10.0.26100.0\cppwinrt\winrt\`, 345 headers including `windows.data.json.h` |
| `cppwinrt.exe` | **Ships in the Windows SDK**: `bin\10.0.26100.0\{x64,arm64}\` |

**The consequence is larger than a package-manager swap.** The plan had assumed the
`Microsoft.Windows.CppWinRT` NuGet package was required for C++/WinRT. It is not — the SDK
supplies both the headers and the projection compiler. Combined with ADR-0005, the
expected steady state is now **zero dependencies**, and `vcpkg.json` should not be created
until a real one appears.

### Honest regression

Dropping NuGet also drops the only automated dependency scanning that was available.
**Dependabot does not support vcpkg**, and GitHub's dependency graph does not parse
`vcpkg.json`. The "no known vulnerabilities" Definition of Done is now enforced manually
(pinned `builtin-baseline` + advisory review). This is recorded as open item 6 and stated
plainly in the skill so nobody reports the gate as automated.

### Also changed

The skill mirrors under `.claude/` and `.codex/` are gitignored, so only
`.github/skills/` is tracked. `tools/sync-skills.sh` is therefore a **post-clone setup
step**, and its `--check` mode is of limited value in CI (it can only check a mirror CI
created itself). `AGENTS.md`, `CONTRIBUTING.md`, `CONTRIBUTING_ja.md`, and ADR-0006 were
corrected accordingly, and the CI task for it was dropped from `TODO.md`.

### Next

`TODO.md` M0 remains open. The immediate next step is creating the solution and the three
projects per ADR-0002 and ADR-0004 — re-deriving `Directory.Build.props` and
`props\syncwingetlink.common.props` in the process — then resolving the open items at the
end of `adr.md`: the `/MT` question (ADR-0003), the winget winmd spike (open item 1), the
vcpkg MSBuild property names (open item 7), and a vcpkg-aware vulnerability scanner
(open item 6).

---

## 2026-07-25 (continued) — Normalize long paths (issue #25)

**Trigger**: a prior agent session on branch `feature/25-normalize-long-paths` was
interrupted before committing. Its working-tree changes to `src/core/Paths.h`,
`src/core/Paths.cpp`, and `tests/SmokeTests.cpp` were recovered, reviewed, and finished
in this session.

### Completed

- `paths::toExtendedLengthPath()`: prefixes a path with `\\?\` (or `\\?\UNC\` for UNC
  paths) so callers can pass long paths to Win32 APIs, per `PLAN.md`'s "Long paths" note.
  Passes through paths already prefixed with `\\?\` or `\\.\` unchanged. Relative paths
  are resolved via `std::filesystem::absolute()` before normalization.
- Added `tests/SmokeTests.cpp::relativeLongPathIsMadeAbsolute`, covering the one branch
  (relative-path input) the recovered tests didn't exercise.
- Checked off the `\\?\` long-path normalization helper item under M1 in `TODO.md`.

### Verified

- `MSBuild.exe syncwingetlink.sln -p:Configuration=Debug -p:Platform=x64 -m`:
  `syncwingetlink.core` and `syncwingetlink.tests` build clean under `/W4 /WX`.
  `syncwingetlink.vcxproj` (the executable) fails with `LNK1561` — pre-existing and
  unrelated: it has no source files yet (`main.cpp` / M6 CLI work hasn't started).
- `vstest.console.exe build\x64\Debug\syncwingetlink.tests.dll /Platform:x64`: all 7
  tests pass, including the new one.

### Deliberately not done

`toExtendedLengthPath` is not wired into `getLinksDirectory`/`getPackagesDirectory` or
any other caller. Issue #25's acceptance criteria and the `TODO.md` M1 checklist ask only
for the helper itself, listed separately from path resolution; the real callers
(`SymlinkService`, `FsScanSource`) don't exist until M2/M5. Wiring it in now would guess
at an interface that isn't designed yet.

Closes #25.

---

## 2026-07-25 (continued) — Fix extended-length re-check after normalization (PR #76 review)

**Trigger**: `copilot-pull-request-reviewer[bot]` flagged that `toExtendedLengthPath()`
only checked the *original* input for a `\\?\`/`\\.\` prefix. If the input were relative
and the current directory were itself `\\?\`-prefixed, `std::filesystem::absolute()`
would produce an already-prefixed absolute path that the code would then misclassify as
UNC (`\\?\...` starts with `\\`), yielding an invalid `\\?\UNC\?\...` result.

### Completed

- `src/core/Paths.cpp`: added a second prefix check on the normalized/absolute path,
  before the UNC-vs-plain-drive decision, so an already-prefixed result (however it got
  that way) is returned unchanged instead of being re-wrapped.
- `tests/SmokeTests.cpp::relativeLongPathUnderExtendedCurrentDirectoryStaysExtended`:
  temporarily sets the process current directory to a `\\?\`-prefixed path (restored via
  an RAII guard) and confirms a relative input still normalizes to a plain `\\?\...`
  path, not `\\?\UNC\...`. Confirms `SetCurrentDirectoryW` does accept a `\\?\`-prefixed
  directory on this machine (Windows 11 24H2), so the scenario is real, not theoretical.

### Verified

- `MSBuild.exe syncwingetlink.sln -p:Configuration=Debug -p:Platform=x64 -m
  -t:syncwingetlink_core,syncwingetlink_tests`: clean, 0 warnings, 0 errors.
- `vstest.console.exe build\x64\Debug\syncwingetlink.tests.dll /Platform:x64`: all 8
  tests pass, including the new regression test.
